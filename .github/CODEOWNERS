# Code owners for sanguinehost/wimse (the `spiffe-id` crate).
# Every PR needs a review from a code owner before it can be merged (when "Require review from Code
# Owners" is on in the branch-protection rules for `main`).
#
# Add maintainers / a team here as the project grows (e.g. `@sanguinehost/maintainers`).
# Keep this in sync with GOVERNANCE.md.

*                       @yokoszn

# The crate itself + the spec-tracking tests — changes here are semver- and security-relevant.
/src/                   @yokoszn
/tests/                 @yokoszn
/Cargo.toml             @yokoszn

# Security-sensitive process docs — changes here need maintainer eyes.
/SECURITY.md            @yokoszn
/docs/playbooks/        @yokoszn
/docs/THREAT-MODEL.md   @yokoszn

# CI / supply-chain config — changes here can affect what gets built and published.
/.github/               @yokoszn
